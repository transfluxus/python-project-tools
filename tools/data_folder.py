from pathlib import Path


def data_folder(path: Path):
    # generate python code that scans the path and generated a python file
    # with BASE_<NAME>_PATH: str = "..."
    # and for p in [...]:
    # p.mkdir(exists_ok=True)
    pass